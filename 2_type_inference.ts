// Type inference
let tempurature = 25; //Typescript infers that tempurature is of type number

let city = "Pune"; //Typescript infers that city is of type string

console.log(tempurature.toFixed(2));
console.log(city.toUpperCase());

//Typescript automatically infers the types of the tempurature and city variables based on thier assigned values.

let isLoggedIn = true;
